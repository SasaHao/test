package com.leeco.ios.util;

/**
 * 读取配置文件中的数据
 * @author haoxiaosha
 *
 */
public class Constant {
	
	//MAC 电脑ip地址
	public static String MAC_IP = ConfigManager.getInstance().getProperty("mac_ip");
	
	//appium配置参数
	public static String DEVICE_NAME = ConfigManager.getInstance().getProperty("deviceName");
	public static String BUNDLEID = ConfigManager.getInstance().getProperty("bundleId");
	public static String UDID = ConfigManager.getInstance().getProperty("udid");
	public static String PLATFORMVERSION = ConfigManager.getInstance().getProperty("platformVersion");
	
	//测试机播放器广告时，观看的电视剧名称
	public static String TV_NAME = ConfigManager.getInstance().getProperty("tv_name");
	public static String TV_NAME1 = ConfigManager.getInstance().getProperty("tv_name1");
	//前贴广告时长
	public static int  PREROALL_AD_TIME = Integer.parseInt(ConfigManager.getInstance().getProperty("prerollAdTime"));
	
	//正片播放多长时间，出现中贴广告
	public static int  MIDROALL_AD_SHOW = Integer.parseInt(ConfigManager.getInstance().getProperty("midrollAdShow"));
	
	//中贴广告时长
	public static int  MIDROALL_AD_TIME = Integer.parseInt(ConfigManager.getInstance().getProperty("midrollAdTime"));
	
	//乐视app账号密码
	public static String USERNAME = ConfigManager.getInstance().getProperty("username");
	public static String PASSWORD = ConfigManager.getInstance().getProperty("password");
	
	//log4j 配置文件
	public static String LOG4J_CONFIG = ConfigManager.getInstance().getProperty("log4jConfigPath");
	
	//fiddler 抓包得到的 log
	public static String FIDDLER_LOG = ConfigManager.getInstance().getProperty("fiddlerLogFile");
	
	//fiddler 抓包，当play请求返回值有protobuf=1时，将结果保存在文件中
	public static String PLAY_RESPONSE_FILE = ConfigManager.getInstance().getProperty("playResponseFile");
		
}
